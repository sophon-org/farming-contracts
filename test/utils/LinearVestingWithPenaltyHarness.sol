// // SPDX-License-Identifier: UNLICENSED
// pragma solidity 0.8.26;

// import {LinearVestingWithPenalty} from "../contracts/token/LinearVestingWithPenalty.sol";

// contract LinearVestingWithPenaltyHarness is LinearVestingWithPenalty {
//     function hasVestingStarted(VestingSchedule storage schedule) external view returns (uint256) {
//         return super._hasVestingStarted(VestingSchedule storage schedule);
//     }

//     function vestedAmount(VestingSchedule storage schedule) external view returns (uint256) {
//         return super._vestedAmount(VestingSchedule storage schedule);
//     }

//     function releasableAmount(VestingSchedule storage schedule) external view returns (uint256) {
//         return super._releasableAmount(VestingSchedule storage schedule);
//     }

//     function releasableAmount(VestingSchedule storage schedule) external view returns (uint256) {
//         return super._releasableAmount(VestingSchedule storage schedule);
//     }
// }